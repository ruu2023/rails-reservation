<%# ユーザーIDと年月（例: "events_user_1_month_2026-05"）ごとに固有のチャンネルを開く %>
<%= turbo_stream_from "events_user_#{current_user.id}_month_#{@start_date.strftime('%Y-%m')}" %>
<%= render "events/calendar", events: @events %>